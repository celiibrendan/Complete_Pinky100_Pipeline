version: '2'
services:
  notebook:
    image: auto-segmentation
    build:
      context: .
    env_file: .env
    environment:
      - DISPLAY=$DISPLAY
      - XSOCK=/tmp/.X11-unix/X0
    ports:
      - "8895:8895"
    volumes:
      - ./notebooks:/notebooks
