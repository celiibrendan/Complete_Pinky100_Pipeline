version: '2'
services:
  notebook:
    image: meshlab3:v4
    build:
      context: .
    env_file: .env
    environment:
      - DISPLAY=$DISPLAY
      - XSOCK=/tmp/.X11-unix/X0
    ports:
      - "8896:8896"
    volumes:
      - ./notebooks:/notebooks
      - ./MeshParty:/MeshParty
